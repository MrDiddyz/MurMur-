import { Card } from "@/components/ui/card";
import { Shell } from "@/components/layout/shell";

export default function ArchivePage() {
  return (
    <Shell title="Archive">
      <Card title="Archive Module">Store notes, reflections, observations, and runs.</Card>
    </Shell>
  );
}
