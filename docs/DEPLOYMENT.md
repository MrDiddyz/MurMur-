# Deployment

The project is configured for Vercel through `vercel.json`.

1. Set the Supabase environment variables in Vercel.
2. Run the SQL in `supabase/schema.sql` in Supabase.
3. Deploy the main branch.
