# MurMur Core

MurMur Core is a minimal operating system for signal intelligence built with Next.js, TypeScript, Supabase, OpenAI, and Zod.

## Setup
1. Copy `.env.example` to `.env.local`.
2. Fill Supabase and OpenAI keys.
3. Install dependencies: `npm install`.
4. Run dev server: `npm run dev`.
