import { SectionShell } from '@/components/murmur/section-shell';

export default function PracticeLabPage() {
  return (
    <SectionShell title="Practice lab" eyebrow="Execution + Reflection">
      Guided practices, weekly challenges, reflection templates, and experimentation logs are designed as implementation loops connected to each module outcome.
    </SectionShell>
  );
}
