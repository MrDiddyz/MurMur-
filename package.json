{
  "name": "murmur-motion-avatar-v0-2",
  "version": "0.2.0",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/api",
    "apps/web",
    "packages/agents",
    "packages/cognitive-core",
    "packages/motion-planner",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm --workspaces run dev",
    "build": "npm --workspaces run build",
    "typecheck": "npm --workspaces run typecheck",
    "verify:web": "npm --workspace @murmur/web run build && npm --prefix apps/web/next-app run lint && npm --prefix apps/web/next-app run typecheck && npm --prefix apps/web/next-app run build",
    "verify": "npm run build && npm --prefix apps/web/next-app run lint && npm --prefix apps/web/next-app run typecheck && npm --prefix apps/web/next-app run build",
    "breach:scan": "python scripts/breach_watch.py",
    "breach:watch": "python scripts/breach_watch.py"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
