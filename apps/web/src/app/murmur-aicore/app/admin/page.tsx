import { SectionShell } from '@/components/murmur/section-shell';

export default function AdminPanelPage() {
  return (
    <SectionShell title="Admin control panel" eyebrow="Operations Center">
      Admin workflows include curriculum management, progress monitoring, subscription oversight, certification approval, referral monitoring, and analytics snapshots.
    </SectionShell>
  );
}
