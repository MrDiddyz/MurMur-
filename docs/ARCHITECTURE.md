# Architecture

MurMur is organized around the Next.js App Router:

- `app/` contains pages and route handlers.
- `components/` contains reusable UI and feature components.
- `lib/` contains Supabase clients and domain logic.
- `content/` is reserved for prompts, examples, and system text.
- `supabase/` contains database schema and migrations.
