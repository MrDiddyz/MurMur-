# Runbook

## Local setup
- Install dependencies for active modules.
- Copy `.env.example` to `.env` when needed.

## Standard checks
- JavaScript/TypeScript: `npm test` and `npm run lint`.
- Python: `ruff check .` and `mypy .` where applicable.

## Release notes
- Summarize user-facing changes.
- Link impacted docs/ADRs.
