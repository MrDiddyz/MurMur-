import { SectionShell } from '@/components/murmur/section-shell';

export default function CertificationSystemPage() {
  return (
    <SectionShell title="Certification system" eyebrow="Assessment Workflow">
      Eligible learners can apply, submit assessments, receive review decisions, and obtain certificates with a verification ID for trust and portability.
    </SectionShell>
  );
}
