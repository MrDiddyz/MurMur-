{
  "name": "murmur-viral-snippet-engine",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "test": "npm run build && node --test tests/**/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
