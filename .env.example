# Server-only secrets (do not expose in client code)
DATABASE_URL="postgresql://user:password@localhost:5432/murmur"
API_SECRET_KEY="replace-me"

# Public values (safe to expose if needed)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
