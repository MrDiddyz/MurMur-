import { SectionShell } from '@/components/murmur/section-shell';

export default function CertificationExplainerPage() {
  return (
    <SectionShell title="Certification pathway" eyebrow="Eligibility → Assessment → Review → Issuance">
      Certifications are unlocked when level requirements and assignment quality thresholds are met. Approved users receive a verifiable certificate ID for public validation.
    </SectionShell>
  );
}
