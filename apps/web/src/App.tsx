export function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">MurMur Web</p>
        <h1>Deployment build is now wired correctly</h1>
        <p>
          This Vite entrypoint lives in the right folder and builds independently
          from legacy Next-style files in this package.
        </p>
      </section>
    </main>
  );
}
