import { SectionShell } from '@/components/murmur/section-shell';

export default function AmbassadorSystemPage() {
  return (
    <SectionShell title="Ambassador system" eyebrow="Referral Engine">
      Members receive referral links, track invited users, monitor conversion rewards, and access an ambassador dashboard for program insights.
    </SectionShell>
  );
}
