{
  "name": "@murmur/orchestrator",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured'"
  },
  "dependencies": {
    "@murmur/graph": "workspace:*",
    "@murmur/shared": "workspace:*",
    "fastify": "^5.1.0"
  }
}
